import Container from '@mui/material/Container';

const Homepage = () => {
  return (
    <Container maxWidth="xl">
      <h1 style={{ paddingTop: 150 }}>Jump Home page</h1>
    </Container>
  );
};

export default Homepage;
