import { PageContainer } from '@/styles/SitePolicy/RefundsReturns.styles';
import { Typography } from '@mui/material';

function RefundsReturns() {
  return (
    <PageContainer>
      <Typography variant="xl4" sx={{ fontWeight: 800 }}>
        Refunds & Returns
      </Typography>
    </PageContainer>
  );
}

export default RefundsReturns;
